# SPEC.md — ระบบทะเบียนอุปกรณ์ไอที

## 1. ชื่อโปรเจคและเป้าหมาย

**IT Asset Tracker** — เว็บแอปหน้าเดียวสำหรับบันทึกทะเบียนอุปกรณ์ไอทีขององค์กรประมาณ 200 ชิ้น (โน้ตบุ๊ก จอ เครื่องพิมพ์ สวิตช์ กล้องวงจรปิด) มาแทนไฟล์ Excel ที่ส่งต่อกันทางอีเมล ซึ่งมีปัญหาสามอย่างคือค้นหาข้อมูลไม่เจอ ไม่รู้ว่าเครื่องไหนอยู่กับใคร และมีหลายเวอร์ชันจนไม่รู้อันไหนล่าสุด เป้าหมายของรอบนี้คือตอบสองคำถามให้ได้ภายในไม่กี่วินาที คือ "เครื่องนี้อยู่กับใครและอยู่ที่ไหน" และ "ประกันของเครื่องไหนกำลังจะหมด" พร้อมเก็บประวัติทุกการเปลี่ยนแปลงไว้ตรวจย้อนหลังได้ ระบบทำงานในเบราว์เซอร์อย่างเดียว ไม่มีเซิร์ฟเวอร์ ไม่มีฐานข้อมูล ไม่มีระบบล็อกอิน

## 2. ผู้ใช้และสถานการณ์การใช้งาน

**ผู้ใช้:** เจ้าหน้าที่ไอทีคนเดียว เปิดใช้บนคอมพิวเตอร์ที่โต๊ะทำงาน ไม่มีผู้ใช้คนอื่นแก้ไขข้อมูลพร้อมกัน

**สถานการณ์การใช้งานหลัก**

| # | สถานการณ์ | สิ่งที่ผู้ใช้ทำ |
|---|---|---|
| 1 | มีคนถามว่าเครื่องนี้อยู่กับใคร | พิมพ์ serial หรือรหัสอุปกรณ์ในช่องค้นหา เห็นผู้ถือครองทันที |
| 2 | พนักงานใหม่มารับเครื่อง | ค้นเครื่องที่สถานะว่าง กดจ่ายออก ใส่ชื่อผู้รับ |
| 3 | พนักงานลาออก คืนของ | ค้นด้วยชื่อคน เห็นทุกเครื่องที่ถืออยู่ กดรับคืนทีละเครื่อง |
| 4 | เครื่องเสีย ต้องส่งซ่อม | เปลี่ยนสถานะเป็นซ่อม ระบบบันทึกประวัติให้อัตโนมัติ |
| 5 | ตรวจสอบประกัน | เปิดหน้าแรก เห็นรายการที่ประกันจะหมดใน 30 และ 90 วัน |
| 6 | ตรวจย้อนหลัง | เปิดหน้ารายละเอียดเครื่อง เห็นประวัติทั้งหมดเรียงตามเวลา |
| 7 | เลิกงานประจำวัน | กด export ไฟล์ JSON เก็บขึ้น Google Drive |

## 3. ฟีเจอร์ที่ต้องมี

### 3.1 จัดการข้อมูลอุปกรณ์
1. ตารางแสดงรายการอุปกรณ์ทั้งหมดในหน้าเดียว
2. ช่องค้นหาช่องเดียว ค้นพร้อมกันได้ทั้ง `asset_code`, `serial_number`, `brand`, `model`, `holder_name`, `location`
3. ตัวกรองตาม `category`, `status`, `holder_name`
4. ฟอร์มเพิ่มอุปกรณ์ใหม่ ใช้ชุดฟิลด์เดียวกันทุกประเภท
5. ฟอร์มแก้ไขอุปกรณ์เดิม
6. สร้าง `asset_code` อัตโนมัติตามประเภท (NB-0001, MON-0001, PRN-0001, SW-0001, CAM-0001) โดยรันเลขต่อจากเลขสูงสุดของประเภทนั้น และไม่อนุญาตให้แก้ไขภายหลัง
7. บังคับกรอก `category`, `brand`, `model`, `serial_number`, `status`, `location`
8. เตือนถ้ากรอก `serial_number` ซ้ำกับเครื่องที่มีอยู่แล้ว

### 3.2 ยืม-คืน
9. ปุ่มจ่ายออก กรอกชื่อผู้รับและแผนก แล้วระบบตั้ง `status` เป็นใช้งาน ตั้ง `holder_name`, `holder_dept`, `issued_date` ให้ในคลิกเดียว
10. ปุ่มรับคืน ล้าง `holder_name`, `holder_dept`, `issued_date` และตั้ง `status` เป็นว่าง พร้อมให้ระบุตำแหน่งที่เก็บใหม่
11. หน้าค้นด้วยชื่อคน แสดงรายการทุกเครื่องที่คนนั้นถืออยู่

### 3.3 ประวัติ
12. ทุกการเปลี่ยนแปลงถูกบันทึกลง `history[]` อัตโนมัติ ผู้ใช้ไม่ต้องพิมพ์เอง
13. บันทึกครบทั้ง เวลา ประเภทการกระทำ ชื่อช่องที่เปลี่ยน ค่าเดิม ค่าใหม่
14. ช่องหมายเหตุให้พิมพ์เพิ่มได้ตอนจ่ายออก รับคืน หรือเปลี่ยนสถานะ
15. ประวัติแก้ไขหรือลบไม่ได้

### 3.4 หน้ารายละเอียด
16. หน้ารายละเอียดรายเครื่อง แสดงข้อมูลปัจจุบันด้านบน ประวัติเรียงจากใหม่ไปเก่าด้านล่าง

### 3.5 การเก็บข้อมูล
17. บันทึกลง `localStorage` อัตโนมัติทุกครั้งที่ข้อมูลเปลี่ยน ไม่ต้องกดบันทึก
18. ปุ่ม Export ดาวน์โหลดข้อมูลทั้งหมดเป็นไฟล์ JSON ตั้งชื่อไฟล์ติดวันที่อัตโนมัติ เช่น `assets-2026-09-09.json`
19. ปุ่ม Import อ่านไฟล์ JSON กลับเข้ามาแทนที่ข้อมูลเดิมทั้งหมด พร้อมกล่องยืนยันก่อนทับ
20. ตรวจสอบโครงสร้างไฟล์ตอน import ถ้าผิดรูปแบบให้ปฏิเสธและไม่แตะข้อมูลเดิม

### 3.6 แจ้งเตือนและสรุป
21. หน้าแรกแสดงรายการที่ `warranty_end` เหลือน้อยกว่า 30 วัน และ 90 วัน
22. หน้าแรกสรุปจำนวนอุปกรณ์แยกตาม `category` และ `status`
23. เตือนบนหน้าจอถ้าไม่ได้กด Export มาเกิน 3 วัน

## 4. ฟีเจอร์ที่อยู่นอกขอบเขตของรอบนี้

| ฟีเจอร์ | เหตุผลที่ตัดออก |
|---|---|
| ระบบล็อกอินและสิทธิ์ผู้ใช้ | ใช้คนเดียว ไม่มีคนอื่นแก้ไข ไม่มีเซิร์ฟเวอร์ให้เก็บบัญชีผู้ใช้ |
| ฐานข้อมูลและ backend | ข้อจำกัดของโปรเจค และ 200 รายการเล็กพอที่จะอยู่ในไฟล์เดียวได้สบาย |
| Import ข้อมูลจาก Excel เดิม | ตั้งใจกรอกใหม่ทั้งหมดเพื่อล้างข้อมูลเก่าที่ไม่น่าเชื่อถือ การเขียนตัวแปลงไฟล์กินเวลาเท่ากับกรอกเอง |
| สติกเกอร์ QR และการสแกนด้วยมือถือ | ใช้บนคอมที่โต๊ะอย่างเดียวในรอบนี้ เพิ่มทีหลังได้โดยไม่ต้องแก้โครงข้อมูล |
| แนบรูปถ่ายเครื่องและใบเสร็จ | ไฟล์รูปทำให้ `localStorage` เต็มเร็วและไฟล์ JSON บวมจนส่งต่อลำบาก |
| ฟิลด์เฉพาะตามประเภท เช่น IP, MAC ของสวิตช์และกล้อง | ใช้ช่อง `note` รองรับไปก่อน การทำ schema แยกตามประเภทเพิ่มความซับซ้อนเกินเวลาที่มี |
| พิมพ์ใบส่งมอบและใบรับคืน | ยังไม่ต้องส่งเอกสารให้ใคร ไม่มีผู้รับปลายทาง |
| Export CSV และรายงานสำหรับบัญชี | ไม่ต้องส่งต่อให้ฝ่ายอื่นในรอบนี้ |
| ค่าเสื่อมราคาและค่าซ่อมสะสม | เก็บ `price` ไว้แล้ว คำนวณเพิ่มทีหลังได้เมื่อมีความต้องการจริง |
| Soft delete และถังขยะ | รอบนี้ใช้การลบตรงพร้อมกล่องยืนยัน ไฟล์ export รายวันทำหน้าที่กู้คืนแทน |
| แก้ผู้ถือครองหลายเครื่องพร้อมกัน | เคสลาออกเกิดไม่บ่อย ทำทีละเครื่องยังรับได้ |

## 5. โครงสร้างข้อมูลของอุปกรณ์ 1 ชิ้น

### 5.1 Asset

| ชื่อช่อง | ชนิดข้อมูล | บังคับ | ตัวอย่าง |
|---|---|---|---|
| `id` | string (uuid) | ✓ | `9f2c1b7a-4d3e-4a11-b8c6-77e0a1d92f34` |
| `asset_code` | string | ✓ | `NB-0042` |
| `category` | enum | ✓ | `notebook` \| `monitor` \| `printer` \| `switch` \| `camera` |
| `brand` | string | ✓ | `Dell` |
| `model` | string | ✓ | `Latitude 5440` |
| `serial_number` | string | ✓ | `7XKQ2P3` |
| `status` | enum | ✓ | `in_use` \| `available` \| `repair` \| `disposed` \| `lost` |
| `holder_name` | string \| null | | `สมชาย ประเสริฐ` |
| `holder_dept` | string \| null | | `ฝ่ายบัญชี` |
| `location` | string | ✓ | `อาคาร A ชั้น 2 ห้อง 205` |
| `issued_date` | string (YYYY-MM-DD) \| null | | `2026-03-11` |
| `purchase_date` | string (YYYY-MM-DD) \| null | | `2025-08-20` |
| `warranty_end` | string (YYYY-MM-DD) \| null | | `2028-08-19` |
| `supplier` | string \| null | | `ร้านคอมพิวเตอร์ ABC` |
| `price` | number \| null | | `24900` |
| `note` | string \| null | | `จอมีรอยมุมซ้าย / IP 192.168.1.24` |
| `created_at` | string (ISO 8601) | ✓ | `2026-09-09T10:12:33+07:00` |
| `updated_at` | string (ISO 8601) | ✓ | `2026-09-09T14:05:11+07:00` |
| `history` | array of HistoryEntry | ✓ | ดูข้อ 5.2 |

### 5.2 HistoryEntry

| ชื่อช่อง | ชนิดข้อมูล | บังคับ | ตัวอย่าง |
|---|---|---|---|
| `at` | string (ISO 8601) | ✓ | `2026-09-09T14:05:11+07:00` |
| `action` | enum | ✓ | `create` \| `update` \| `issue` \| `return` \| `to_repair` \| `from_repair` \| `dispose` |
| `field` | string \| null | | `holder_name` |
| `from` | string \| null | | `null` |
| `to` | string \| null | | `สมชาย ประเสริฐ` |
| `note` | string \| null | | `ยืมใช้ระหว่างเครื่องเดิมส่งซ่อม` |

### 5.3 ไฟล์ Export

| ชื่อช่อง | ชนิดข้อมูล | ตัวอย่าง |
|---|---|---|
| `schema_version` | number | `1` |
| `exported_at` | string (ISO 8601) | `2026-09-09T17:30:00+07:00` |
| `assets` | array of Asset | `[ ... ]` |

## 6. เทคโนโลยีที่ใช้และเหตุผล

| เทคโนโลยี | เหตุผล |
|---|---|
| ไฟล์ HTML เดียวจบ | เปิดจากไฟล์ในเครื่องได้เลย ไม่ต้องติดตั้งอะไร ย้ายเครื่องแค่ก๊อปไฟล์เดียว |
| Vanilla JavaScript ไม่มี build step | ไม่มี npm ไม่มี bundler ไม่มีเวลาเสียไปกับ toolchain แก้โค้ดแล้วรีเฟรชเห็นผลทันที |
| ไม่ใช้ framework | ขนาดงานเป็นตารางกับฟอร์ม ไม่คุ้มกับเวลาที่ต้องเสียไปตั้งค่า และไม่ต้องพึ่ง CDN ตอนใช้งาน |
| `localStorage` | ที่เก็บข้อมูลระหว่างทำงาน ไม่ต้องกดบันทึก ปิดเบราว์เซอร์แล้วเปิดใหม่ข้อมูลยังอยู่ |
| ไฟล์ JSON คือแหล่งข้อมูลจริง | `localStorage` หายได้ ไฟล์ JSON ที่ export เก็บขึ้น Drive คือสำเนาที่เชื่อถือได้ |
| CSS เขียนเอง ไม่ใช้ framework | หน้าจอมีไม่กี่หน้าและใช้บนเดสก์ท็อปอย่างเดียว ไม่ต้องการ responsive ซับซ้อน |

## 7. เกณฑ์ว่างานนี้เสร็จ

1. เพิ่มอุปกรณ์ใหม่ 1 ชิ้นได้ครบทุกช่อง และได้ `asset_code` ที่รันเลขต่อจากเครื่องล่าสุดของประเภทนั้นถูกต้อง
2. กรอก `serial_number` ซ้ำกับเครื่องที่มีอยู่แล้ว ระบบเตือนก่อนบันทึก
3. เว้นช่องบังคับว่างแล้วกดบันทึก ระบบไม่ยอมบันทึกและชี้ว่าช่องไหนขาด
4. พิมพ์ serial บางส่วนในช่องค้นหา ผลลัพธ์ขึ้นตรงเครื่อง
5. พิมพ์ชื่อคนในช่องค้นหา เห็นครบทุกเครื่องที่คนนั้นถืออยู่
6. กรองตาม `category` และ `status` พร้อมกันได้ และจำนวนผลลัพธ์ตรงกับที่นับด้วยมือ
7. กดจ่ายออกให้เครื่องที่สถานะว่าง แล้ว `status`, `holder_name`, `holder_dept`, `issued_date` เปลี่ยนครบทั้งสี่ช่องในคลิกเดียว
8. กดรับคืน แล้ว `holder_name`, `holder_dept`, `issued_date` ถูกล้าง และ `status` กลับเป็นว่าง
9. เปิดหน้ารายละเอียดหลังทำข้อ 7 และ 8 เห็นรายการประวัติเพิ่มขึ้นครบทั้งสองครั้ง พร้อมค่าเดิมและค่าใหม่ที่ถูกต้อง
10. ประวัติเรียงจากใหม่ไปเก่า และไม่มีทางแก้หรือลบผ่านหน้าจอ
11. แก้ข้อมูล ปิดเบราว์เซอร์ เปิดใหม่ ข้อมูลยังอยู่ครบ
12. กด Export ได้ไฟล์ JSON ที่มี `schema_version`, `exported_at`, `assets` ครบ และเปิดอ่านด้วย text editor ได้
13. ล้าง `localStorage` แล้ว Import ไฟล์ที่ export ไว้ ข้อมูลกลับมาครบทั้งหมดรวมถึงประวัติทุกรายการ
14. Import ไฟล์ที่ผิดรูปแบบ ระบบปฏิเสธและข้อมูลเดิมในหน้าจอไม่เปลี่ยน
15. ตั้ง `warranty_end` ของเครื่องหนึ่งให้เหลือ 20 วัน อีกเครื่องเหลือ 60 วัน หน้าแรกจัดทั้งสองเครื่องเข้ากลุ่ม 30 วันและ 90 วันได้ถูกต้อง
16. ตัวเลขสรุปตาม `category` และ `status` บนหน้าแรกตรงกับจำนวนแถวในตารางเมื่อกรองด้วยเงื่อนไขเดียวกัน
17. ใส่ข้อมูลทดสอบ 200 รายการ แล้วค้นหาและกรองยังตอบสนองทันที ไม่มีอาการหน่วง
18. เปิดใช้งานโดยไม่ต่ออินเทอร์เน็ต ทุกฟีเจอร์ยังทำงานครบ

## 8. ข้อจำกัดที่รู้ตัวอยู่แล้ว

1. `localStorage` ผูกกับเบราว์เซอร์และเครื่องที่ใช้ ล้างข้อมูลเบราว์เซอร์หรือเผลอเปิดโหมด incognito แล้วข้อมูลหาย ต้องอาศัยวินัยการ export ทุกครั้งที่เลิกงาน
2. ไม่มีระบบสำรองข้อมูลอัตโนมัติ ความปลอดภัยของข้อมูลขึ้นอยู่กับคนกดปุ่ม Export ทั้งหมด
3. ใช้ได้ทีละคน ถ้าเปิดสองเครื่องแล้วแก้พร้อมกัน ไฟล์ที่ import ทับทีหลังจะลบงานของอีกฝั่งทิ้ง
4. ไม่มีระบบล็อกอิน ใครเปิดไฟล์ได้ก็แก้ข้อมูลได้ ต้องอาศัยการควบคุมการเข้าถึงเครื่องแทน
5. ประวัติบันทึกได้แค่ว่าเปลี่ยนอะไรเมื่อไหร่ ไม่รู้ว่าใครเป็นคนเปลี่ยน เพราะไม่มีบัญชีผู้ใช้
6. อุปกรณ์เครือข่ายอย่างสวิตช์และกล้องยังไม่มีช่องเก็บ IP และ MAC เป็นเรื่องเป็นราว ต้องพิมพ์ลงช่อง `note` ซึ่งค้นหาได้แต่กรองไม่ได้
7. การกรอกข้อมูล 200 ชิ้นด้วยมือใช้เวลาประมาณ 7 ถึง 10 ชั่วโมง เป็นงานแยกที่ไม่รวมอยู่ในเวลาพัฒนา 2 วัน
8. ข้อมูลเก่าใน Excel จะไม่ถูกโอนมา ประวัติของอุปกรณ์ทุกชิ้นเริ่มนับใหม่จากวันที่กรอกเข้าระบบ
9. ยังไม่รองรับการใช้งานบนมือถือ ต้องกลับมาที่โต๊ะเพื่อบันทึกข้อมูลหลังเดินตรวจหน้างาน
10. เมื่อจำนวนอุปกรณ์โตเกินประมาณ 1,000 ชิ้น หรือมีคนใช้มากกว่าหนึ่งคน สถาปัตยกรรมนี้จะไม่พอ ต้องย้ายขึ้นเซิร์ฟเวอร์พร้อมฐานข้อมูลจริง
